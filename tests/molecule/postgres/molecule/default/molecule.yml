---
# Testing goal: verify the postgres role installs PG packages, lays down
# the vendor filesystem, masks the vendor systemd unit, and registers the
# SELinux port label. No patroni / cluster bootstrap is exercised here.
driver:
  name: podman
platforms:
  - name: pigsty-lite-postgres
    image: localhost/molecule-base-data:latest
    pre_build_image: true
    privileged: true
    command: /usr/sbin/init
    volume_mounts:
      - "/sys/fs/cgroup:/sys/fs/cgroup:rw"
    capabilities: [SYS_ADMIN]
    security_opts: ["label=disable"]
    systemd: always
    groups: [postgres]
    network: pigsty-lite-postgres-default
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        cluster_name: pigsty-lite-test
        cluster_domain: test.local
        pigsty_pki_dir: /etc/pki/pigsty
        postgres_version: 18
        postgres_port: 5432
    host_vars:
      pigsty-lite-postgres:
        postgres_role: primary
verifier:
  name: ansible
scenario:
  test_sequence:
    - destroy
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy
