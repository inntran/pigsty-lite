---
# backup_store group defaults.
# Role defaults in roles/backup_store/defaults/main.yml cover everything;
# this file exists as the documented override point for operators who
# split backup_store onto its own host (e.g. a different firewalld zone
# or backup store mount path).
