---
# group_vars/etcd.yml - per-cluster overrides for the etcd group.
# `initial-cluster` is rendered inline in roles/etcd/templates/etcd.conf.yml.j2
# from groups['etcd'], so it is consistent across all members without
# any control-side variable assembly.

etcd_initial_cluster_state: new
